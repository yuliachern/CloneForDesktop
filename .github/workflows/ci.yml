name: Node.js CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
<<<<<<< HEAD

=======
>>>>>>> 9613dd0 (Fix CI path after moving project to root)
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
<<<<<<< HEAD

=======
>>>>>>> 9613dd0 (Fix CI path after moving project to root)
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Start Redis
        run: |
          sudo apt-get update
          sudo apt-get install -y redis-server
          sudo systemctl start redis
          redis-cli ping

      - name: Run tests
        run: npm test
